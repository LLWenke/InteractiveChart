package com.wk.chart.enumeration;

/**
 * <p>ExtremumTagDrawableLocation</p>
 * 极值标签的Drawable显示位置
 */

public interface ExtremumTagDrawableLocation {
    int MAX = 0;//MAX标签显示
    int MIN = 1;//MIN标签显示
    int ALL = 2;//MAX和MIN都显示
}
